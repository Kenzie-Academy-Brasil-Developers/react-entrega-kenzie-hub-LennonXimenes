export function StacksCard() {
    return (
        <li>
            <div>
                <img src={} alt={} />
                <FontTitle>{}</FontTitle>
            </div>
            <button onClick={() => removeProductFromList()}></button>
        </li>
    )
}